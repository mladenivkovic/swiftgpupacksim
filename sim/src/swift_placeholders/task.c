/**
 * Task data.
 */

#include "task.h"

const char* taskID_names[task_type_count] = {"none", "pack", "unpack"};

const char* subtaskID_names[task_subtype_count] = {"none", "density",
                                                   "gradient", "force"};
