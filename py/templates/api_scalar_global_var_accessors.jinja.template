{# deliberately empty line #}
/**
 * @brief get {{NAME}}{% if HAS_DOC %}, {{DOC}}{% endif %}.
 */
{%- if HAS_IFDEF and IS_STRUCT %}
{#- we can't return a struct which is undefined because it's hidden behind a macro, so we have to hide the entire API. #}
#ifdef {{IFDEF}}
{%- endif %}
static __attribute__((always_inline)) INLINE {{TYPE}}
  part_get_{{NAME}}{{API_SUFFIX}}(const size_t pind) {

{%- if HAS_IFDEF and not IS_STRUCT %}
#ifdef {{IFDEF}}
{%- endif %}

  const struct {{ROOT_STRUCT}}* restrict {{ROOT_STRUCT}}_s = global_hydro_part_arrays._{{ROOT_STRUCT}} + pind;

    {%- if DEBUG_ID_CHECKS %}
{% if DEBUG_ID_CHECKS_MACRO_GUARD -%} #ifdef SWIFT_DEBUG_CHECKS {%- endif %}
  /* Forbid ID = 0 to prevent false positives by forgotten initialisation */
  const struct part* restrict p = global_hydro_part_arrays._part + pind;
  swift_assert(p->_accessor_id != 0);
  /* Make sure we're accessing the correct data */
  if({{ROOT_STRUCT}}_s->_accessor_id != p->_accessor_id)
    error("Accessor IDs not equal: %lld %lld", {{ROOT_STRUCT}}_s->_accessor_id, p->_accessor_id);
{% if DEBUG_ID_CHECKS_MACRO_GUARD -%} #endif {%- endif %}
    {%- endif %}

{%- if HAS_PARENT_STRUCT %}
  return {{ROOT_STRUCT}}_s->_{{PARENT_STRUCT}}._{{NAME}};
{%- else %}
  return {{ROOT_STRUCT}}_s->_{{NAME}};
{%- endif -%}

{%- if HAS_IFDEF and not IS_STRUCT %} {# return some default val if function contents are hidden behind macro #}
#else
  return {{IFDEF_RETURN_VAL}};
#endif
{%- endif %}
}
{% if HAS_IFDEF and IS_STRUCT -%}
#endif
{%- endif %}

/**
 * @brief get a pointer to {{NAME}}{% if HAS_DOC %}, {{DOC}}{% endif %}.
 * Use this only if you need to modify the value, i.e. if you need write access
 * to {{NAME}}. If you need read-only access to {{NAME}}, use part_get_const_{{NAME}}_p() instead.
 */
{%- if HAS_IFDEF and IS_STRUCT %}
{#- we can't return a struct which is undefined because it's hidden behind a macro, so we have to hide the entire API. #}
#ifdef {{IFDEF}}
{%- endif %}
static __attribute__((always_inline)) INLINE {{TYPE}}*
  part_get_{{NAME}}_p{{API_SUFFIX}}(const size_t pind) {

{%- if HAS_IFDEF and not IS_STRUCT %}
#ifdef {{IFDEF}}
{%- endif %}

  struct {{ROOT_STRUCT}}* restrict {{ROOT_STRUCT}}_s = global_hydro_part_arrays._{{ROOT_STRUCT}} + pind;

    {%- if DEBUG_ID_CHECKS %}
{% if DEBUG_ID_CHECKS_MACRO_GUARD -%} #ifdef SWIFT_DEBUG_CHECKS {%- endif %}
  /* Forbid ID = 0 to prevent false positives by forgotten initialisation */
  const struct part* restrict p = global_hydro_part_arrays._part + pind;
  swift_assert(p->_accessor_id != 0);
  /* Make sure we're accessing the correct data */
  if({{ROOT_STRUCT}}_s->_accessor_id != p->_accessor_id)
    error("Accessor IDs not equal: %lld %lld", {{ROOT_STRUCT}}_s->_accessor_id, p->_accessor_id);
{% if DEBUG_ID_CHECKS_MACRO_GUARD -%} #endif {%- endif %}
    {%- endif %}

{%- if HAS_PARENT_STRUCT %}
  return &{{ROOT_STRUCT}}_s->_{{PARENT_STRUCT}}._{{NAME}};
{%- else %}
  return &{{ROOT_STRUCT}}_s->_{{NAME}};
{%- endif -%}

{%- if HAS_IFDEF and not IS_STRUCT %}
#else
  return NULL;
#endif
{%- endif %}
}
{% if HAS_IFDEF and IS_STRUCT -%}
#endif
{%- endif %}
{%- if not IS_POINTER %}

/**
 * @brief get read-only access to pointer to {{NAME}}{% if not HAS_DOC %}.{% else %},
 * {{DOC}}.{% endif %}
 * If you need write access to {{NAME}}, use part_get_{{NAME}}_p() instead.
 */
{%- if HAS_IFDEF and IS_STRUCT %}
{#- we can't return a struct which is undefined because it's hidden behind a macro, so we have to hide the entire API. #}
#ifdef {{IFDEF}}
{%- endif %}
static __attribute__((always_inline)) INLINE const {{TYPE}}*
  part_get_const_{{NAME}}_p{{API_SUFFIX}}(const size_t pind) {

{%- if HAS_IFDEF and not IS_STRUCT %}
#ifdef {{IFDEF}}
{%- endif %}

  const struct {{ROOT_STRUCT}}* restrict {{ROOT_STRUCT}}_s = global_hydro_part_arrays._{{ROOT_STRUCT}} + pind;

    {%- if DEBUG_ID_CHECKS %}
{% if DEBUG_ID_CHECKS_MACRO_GUARD -%} #ifdef SWIFT_DEBUG_CHECKS {%- endif %}
  /* Forbid ID = 0 to prevent false positives by forgotten initialisation */
  const struct part* restrict p = global_hydro_part_arrays._part + pind;
  swift_assert(p->_accessor_id != 0);
  /* Make sure we're accessing the correct data */
  if({{ROOT_STRUCT}}_s->_accessor_id != p->_accessor_id)
    error("Accessor IDs not equal: %lld %lld", {{ROOT_STRUCT}}_s->_accessor_id, p->_accessor_id);
{% if DEBUG_ID_CHECKS_MACRO_GUARD -%} #endif {%- endif %}
    {%- endif %}

{%- if HAS_PARENT_STRUCT %}
  return &{{ROOT_STRUCT}}_s->_{{PARENT_STRUCT}}._{{NAME}};
{%- else %}
  return &{{ROOT_STRUCT}}_s->_{{NAME}};
{%- endif -%}

{%- if HAS_IFDEF and not IS_STRUCT %}
#else
  return NULL;
#endif
{%- endif %}
}
{% if HAS_IFDEF and IS_STRUCT -%}
#endif
{%- endif %}

{% endif -%}
/**
 * @brief set the value of {{NAME}}{% if HAS_DOC %}, {{DOC}}{% endif %}.
 */
{%- if HAS_IFDEF and IS_STRUCT %}
{#- we can't return a struct which is undefined because it's hidden behind a macro, so we have to hide the entire API. #}
#ifdef {{IFDEF}}
{%- endif %}
static __attribute__((always_inline)) INLINE void
  part_set_{{NAME}}{{API_SUFFIX}}(const size_t pind, {% if not IS_POINTER -%} const {%- endif %} {{TYPE}} {{NAME}}) {

{%- if HAS_IFDEF and not IS_STRUCT %}
#ifdef {{IFDEF}}
{%- endif %}

  struct {{ROOT_STRUCT}}* restrict {{ROOT_STRUCT}}_s = global_hydro_part_arrays._{{ROOT_STRUCT}} + pind;

    {%- if DEBUG_ID_CHECKS %}
{% if DEBUG_ID_CHECKS_MACRO_GUARD -%} #ifdef SWIFT_DEBUG_CHECKS {%- endif %}
  /* Forbid ID = 0 to prevent false positives by forgotten initialisation */
  const struct part* restrict p = global_hydro_part_arrays._part + pind;
  swift_assert(p->_accessor_id != 0);
  /* Make sure we're accessing the correct data */
  if({{ROOT_STRUCT}}_s->_accessor_id != p->_accessor_id)
    error("Accessor IDs not equal: %lld %lld", {{ROOT_STRUCT}}_s->_accessor_id, p->_accessor_id);
{% if DEBUG_ID_CHECKS_MACRO_GUARD -%} #endif {%- endif %}
    {%- endif %}

{%- if HAS_PARENT_STRUCT %}
  {{ROOT_STRUCT}}_s->_{{PARENT_STRUCT}}._{{NAME}} = {{NAME}};
{%- else %}
  {{ROOT_STRUCT}}_s->_{{NAME}} = {{NAME}};
{%- endif -%}

{%- if HAS_IFDEF and not IS_STRUCT %}
#endif
{%- endif %}
}
{% if HAS_IFDEF and IS_STRUCT -%}
#endif
{%- endif %}
{# deliberately empty line #}
