{%- if HAS_IFDEF -%}
    #ifdef {{IFDEF}}
{% endif -%}

{% if HAS_DOC -%}
{% for i in range(INDENT_LEVEL) %}  {% endfor -%} {#- adds indent -#}
    /*! {{DOC}} */
{% endif -%} 

{% for i in range(INDENT_LEVEL) %}  {% endfor %} {#- adds indent -#}
{{TYPE}} _{{NAME}}

{%- if IS_ARRAY -%}
	{%- if IS_MULTIDIM_ARRAY -%}
	  {%- for entry in MULTIDIM_ARRAY_DIMENSIONS -%}
			[{{entry}}] 
		{%- endfor -%}
	{%- else -%}
    [{{SIZE}}] 
	{%- endif -%}
{%- endif -%};

{%- if HAS_IFDEF %}
#endif
{%- endif %}
{# deliberately empty line #}
