{%- if HAS_IFDEF -%}
    #ifdef {{IFDEF}}
{% endif -%}

{% if HAS_DOC -%}
{% for i in range(INDENT_LEVEL) %}  {% endfor -%} {#- adds indent -#}
    /*! {{DOC}} */
{% endif -%} 

{% for i in range(INDENT_LEVEL) %}  {% endfor %} {#- adds indent -#}
{{TYPE}} _{{NAME}}

{%- if IS_ARRAY -%}
    [{{SIZE}}] 
{%- endif -%};

{%- if HAS_IFDEF %}
#endif
{%- endif %}
{# deliberately empty line #}
